#!/bin/bash
whatsappgo="/usr/local/webchat/bin/whatsapp.go"
command="/usr/local/webchat/bin/whatsapp.bin"

if [ -f "$whatsappgo" ]
then
	command="go run $whatsappgo"
fi

while true
do
	$command $@
	sleep 5
done
